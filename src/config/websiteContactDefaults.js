/**
 * Default `contact` bila API tidak mengirim field (merge ke config di websitePublicDataCache).
 * Bentuk respon: hanya tiga kunci berikut.
 */
export const WEBSITE_CONTACT_RESPONSE_DUMMY = {
  whatsapp: '#',
  telegram: '',
  testimoni: '',
}
