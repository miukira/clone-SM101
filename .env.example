# Aset publik (banner, logo path dari API, dll.): base URL absolut tanpa trailing slash.
# Kosong = pakai window.location.origin (dev/prod same host).
# VITE_PUBLIC_ASSET_BASE_URL=https://cdn.example.com
